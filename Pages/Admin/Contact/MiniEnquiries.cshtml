@page
@model TaxHelperToday.Pages.Admin.Contact.MiniEnquiriesModel
@{
    ViewData["Title"] = "Mini Enquiries";
}

@section Styles {
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.7/css/dataTables.bootstrap4.min.css">
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Mini Enquiries</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/Admin/Dashboard">Home</a></li>
                    <li class="breadcrumb-item"><a href="#">Contact</a></li>
                    <li class="breadcrumb-item active">Mini Enquiries</li>
                </ol>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container-fluid">
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                @TempData["SuccessMessage"]
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        }

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">All Mini Enquiries</h3>
                        <div class="card-tools">
                            <div class="input-group input-group-sm" style="width: 200px;">
                                <div class="input-group-prepend">
                                    <label class="input-group-text" for="statusFilter">Status:</label>
                                </div>
                                <select id="statusFilter" class="form-control">
                                    @{
                                        var isAllSelected = string.IsNullOrEmpty(Model.SelectedStatus) || Model.SelectedStatus == "All";
                                        var isNewSelected = Model.SelectedStatus == "New";
                                        var isContactedSelected = Model.SelectedStatus == "Contacted";
                                        var isConvertedSelected = Model.SelectedStatus == "Converted";
                                        var isClosedSelected = Model.SelectedStatus == "Closed";
                                    }
                                    @if (isAllSelected)
                                    {
                                        <option value="All" selected>All</option>
                                    }
                                    else
                                    {
                                        <option value="All">All</option>
                                    }
                                    @if (isNewSelected)
                                    {
                                        <option value="New" selected>New</option>
                                    }
                                    else
                                    {
                                        <option value="New">New</option>
                                    }
                                    @if (isContactedSelected)
                                    {
                                        <option value="Contacted" selected>Contacted</option>
                                    }
                                    else
                                    {
                                        <option value="Contacted">Contacted</option>
                                    }
                                    @if (isConvertedSelected)
                                    {
                                        <option value="Converted" selected>Converted</option>
                                    }
                                    else
                                    {
                                        <option value="Converted">Converted</option>
                                    }
                                    @if (isClosedSelected)
                                    {
                                        <option value="Closed" selected>Closed</option>
                                    }
                                    else
                                    {
                                        <option value="Closed">Closed</option>
                                    }
                                </select>
                            </div>
                        </div>
                    </div>
                    <div class="card-body">
                        <table id="miniEnquiriesTable" class="table table-bordered table-striped">
                            <thead>
                                <tr>
                                    <th>Sln</th>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>User Type</th>
                                    <th>Status</th>
                                    <th>Created</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @{ int sln = 1; }
                                @foreach (var enquiry in Model.MiniEnquiries)
                                {
                                    <tr>
                                        <td>@(sln++)</td>
                                        <td>@enquiry.Name</td>
                                        <td>@enquiry.Email</td>
                                        <td>
                                            @if (!string.IsNullOrEmpty(enquiry.UserType))
                                            {
                                                <span class="badge badge-info">@enquiry.UserType</span>
                                            }
                                            else
                                            {
                                                <span>-</span>
                                            }
                                        </td>
                                        <td>
                                            @switch (enquiry.Status?.ToLower())
                                            {
                                                case "new":
                                                    <span class="badge badge-primary">New</span>
                                                    break;
                                                case "contacted":
                                                    <span class="badge badge-info">Contacted</span>
                                                    break;
                                                case "converted":
                                                    <span class="badge badge-success">Converted</span>
                                                    break;
                                                case "closed":
                                                    <span class="badge badge-secondary">Closed</span>
                                                    break;
                                                default:
                                                    <span class="badge badge-secondary">@enquiry.Status</span>
                                                    break;
                                            }
                                        </td>
                                        <td>@enquiry.CreatedAt.ToString("MMM dd, yyyy HH:mm")</td>
                                        <td>
                                            <button type="button" class="btn btn-sm btn-info" data-toggle="modal" data-target="#enquiryModal@(enquiry.Id)" title="View Details">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                            <a href="/Admin/Contact/MiniEnquiries/Details/@enquiry.Id" class="btn btn-sm btn-primary" title="Open Details Page">
                                                <i class="fas fa-external-link-alt"></i>
                                            </a>
                                            <div class="btn-group">
                                                <button type="button" class="btn btn-sm btn-secondary dropdown-toggle" data-toggle="dropdown">
                                                    Status
                                                </button>
                                                <div class="dropdown-menu">
                                                    <form method="post" asp-page-handler="UpdateStatus" asp-route-id="@enquiry.Id" asp-route-status="New" class="d-inline">
                                                        <button type="submit" class="dropdown-item">New</button>
                                                    </form>
                                                    <form method="post" asp-page-handler="UpdateStatus" asp-route-id="@enquiry.Id" asp-route-status="Contacted" class="d-inline">
                                                        <button type="submit" class="dropdown-item">Contacted</button>
                                                    </form>
                                                    <form method="post" asp-page-handler="UpdateStatus" asp-route-id="@enquiry.Id" asp-route-status="Converted" class="d-inline">
                                                        <button type="submit" class="dropdown-item">Converted</button>
                                                    </form>
                                                    <form method="post" asp-page-handler="UpdateStatus" asp-route-id="@enquiry.Id" asp-route-status="Closed" class="d-inline">
                                                        <button type="submit" class="dropdown-item">Closed</button>
                                                    </form>
                                                </div>
                                            </div>
                                            <form method="post" asp-page-handler="Delete" asp-route-id="@enquiry.Id" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this mini enquiry?');">
                                                <button type="submit" class="btn btn-sm btn-danger" title="Delete">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>

                                    <!-- Modal for mini enquiry details -->
                                    <div class="modal fade" id="enquiryModal@(enquiry.Id)" tabindex="-1" role="dialog">
                                        <div class="modal-dialog modal-lg" role="document">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title">Mini Enquiry Details</h5>
                                                    <button type="button" class="close" data-dismiss="modal">
                                                        <span>&times;</span>
                                                    </button>
                                                </div>
                                                <div class="modal-body">
                                                    <dl class="row">
                                                        <dt class="col-sm-3">Name:</dt>
                                                        <dd class="col-sm-9">@enquiry.Name</dd>

                                                        <dt class="col-sm-3">Email:</dt>
                                                        <dd class="col-sm-9">@enquiry.Email</dd>

                                                        <dt class="col-sm-3">User Type:</dt>
                                                        <dd class="col-sm-9">
                                                            @if (!string.IsNullOrEmpty(enquiry.UserType))
                                                            {
                                                                <span class="badge badge-info">@enquiry.UserType</span>
                                                            }
                                                            else
                                                            {
                                                                <span>-</span>
                                                            }
                                                        </dd>

                                                        <dt class="col-sm-3">Status:</dt>
                                                        <dd class="col-sm-9">
                                                            @switch (enquiry.Status?.ToLower())
                                                            {
                                                                case "new":
                                                                    <span class="badge badge-primary">New</span>
                                                                    break;
                                                                case "contacted":
                                                                    <span class="badge badge-info">Contacted</span>
                                                                    break;
                                                                case "converted":
                                                                    <span class="badge badge-success">Converted</span>
                                                                    break;
                                                                case "closed":
                                                                    <span class="badge badge-secondary">Closed</span>
                                                                    break;
                                                                default:
                                                                    <span class="badge badge-secondary">@enquiry.Status</span>
                                                                    break;
                                                            }
                                                        </dd>

                                                        <dt class="col-sm-3">Assigned To:</dt>
                                                        <dd class="col-sm-9">@(enquiry.AssignedToName ?? "Unassigned")</dd>

                                                        @if (!string.IsNullOrEmpty(enquiry.Notes))
                                                        {
                                                            <dt class="col-sm-3">Notes:</dt>
                                                            <dd class="col-sm-9">
                                                                <div class="border p-2 bg-light">
                                                                    @enquiry.Notes
                                                                </div>
                                                            </dd>
                                                        }

                                                        <dt class="col-sm-3">Created:</dt>
                                                        <dd class="col-sm-9">@enquiry.CreatedAt.ToString("MMM dd, yyyy HH:mm")</dd>

                                                        <dt class="col-sm-3">Last Updated:</dt>
                                                        <dd class="col-sm-9">@enquiry.UpdatedAt.ToString("MMM dd, yyyy HH:mm")</dd>
                                                    </dl>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script src="https://cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.7/js/dataTables.bootstrap4.min.js"></script>
    <script>
        $(function () {
            var table = $("#miniEnquiriesTable").DataTable({
                "responsive": true,
                "lengthChange": true,
                "autoWidth": false,
                "order": [[5, "desc"]], // Sort by Created date descending (column index updated due to Sln column)
                "columnDefs": [
                    { "orderable": false, "targets": [6] } // Disable sorting on Actions column
                ]
            });

            // Handle status filter change
            $("#statusFilter").on("change", function () {
                var selectedStatus = $(this).val();
                var url = new URL(window.location.href);
                
                if (selectedStatus === "All" || selectedStatus === "") {
                    url.searchParams.delete("status");
                } else {
                    url.searchParams.set("status", selectedStatus);
                }
                
                window.location.href = url.toString();
            });
        });
    </script>
}
